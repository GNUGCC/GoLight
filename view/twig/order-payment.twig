<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <form action="{{ result.parameters.url }}" id="form" method="post">
{% for name, value in result.parameters %}
            <input name="{{ name }}" type="hidden" value="{{ value }}">
{% endfor %}
        </form>
        <script>
            form.submit();
        </script>
    </body>
</html>
